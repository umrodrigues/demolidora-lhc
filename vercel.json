{
  "buildCommand": "git lfs pull && pnpm run build",
  "installCommand": "git lfs install && pnpm install",
  "functions": {
    "src/app/**/*.tsx": {
      "maxDuration": 30
    }
  },
  "build": {
    "env": {
      "NODE_VERSION": "18"
    }
  }
}